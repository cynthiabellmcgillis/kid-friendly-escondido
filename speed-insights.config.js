module.exports = {
  // Configuration options
  logLevel: 'debug', // Optional: Set the log level (default is 'info')
  sampleRate: 100, // Optional: Set the sample rate (default is 100)
  // Add more configuration options as needed
}
